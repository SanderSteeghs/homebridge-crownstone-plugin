export interface CrownstonePlatformConfig {
    sphereName?: string,
    crownstoneUsername?: string,
    crownstonePassword?: string,
    uartDevice?: string,
    v1CloudUrl?: string,
    v2CloudUrl?: string
    sseCloudUrl?: string,
}